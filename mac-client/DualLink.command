#!/usr/bin/env bash
# DualLink.command — Double-click this file in Finder to build & launch DualLink.
# macOS recognises .command files and opens them in Terminal automatically.
cd "$(dirname "$0")"
exec ./run_app.sh
